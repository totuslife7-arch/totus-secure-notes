/** Hidden developer unlock — not for production store listings. */
export const DEV_UNLOCK_SECRET = 'TOTUS-DEV-2026';

export const DEV_UNLOCK_STORE_KEY = 'dev_unlock_active';

/** Taps on version label in Settings → About to reveal code entry. */
export const DEV_UNLOCK_VERSION_TAP_COUNT = 7;
