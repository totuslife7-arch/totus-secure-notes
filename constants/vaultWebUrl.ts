import { POLICY_HOSTING_BASE } from '@/constants/policyUrls';

/** Read-only web vault viewer — decrypts `.totus` exports in the browser only. */
export const VAULT_WEB_URL = `${POLICY_HOSTING_BASE}/vault`;
